# Vue example

A complete app using the published free package, without Tailwind. Use Node 24, then `npm install`, `npm run dev` or `npm run build` in this directory.

The pilot uses the public archive while npm owner verification is pending. After npm publication, you can replace the archive URL with `0.1.0`.
